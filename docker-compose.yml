services:

  backend-dev:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    ports:
      - "5000:5000"
    volumes:
      - ./inventory-backend:/app
    environment:
      - NODE_ENV=development
      - MONGO_URI=mongodb+srv://user1:123123123123@fimoney.stbkaqy.mongodb.net/?retryWrites=true&w=majority&appName=FiMoney
    depends_on:
      - db
  db:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - db_data:/data/db

volumes:
  db_data:
